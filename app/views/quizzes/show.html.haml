
%h1 Quiz Show Page


%table
  %tr
    %th Name
    %th Last Date
    %th Last Score
    %th Avg Score
    %th Number of Attempts
    %tr
      %td= @quiz.name
      %td= q.name
      %td- q.results.each do |result|
      %td= re.score
      / %td=/{:'data-created' => }
      / %td/ =
      / %td/ =

= @quiz.name
