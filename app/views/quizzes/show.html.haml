%h1 Quiz Show Page


%table
  %tr
    %th Name
    %th Last Date
    %th Last Score
    %th Avg Score
    %th Number of Attempts
  - @quizzes.each do |q|
    %tr
      %td= q.name
      %td= q.name
      / %td=/{:'data-created' => }
      / %td/ =
      / %td/ =
